# Package `server`

This package hosts protocol implementations to expose and serve ADK agents.

## Sub-packages

*   **`adkrest`**: Implements a standard REST API for interacting with agents (sessions, runs, artifacts).
*   **`adka2a`**: Implements the Agent-to-Agent (A2A) protocol for inter-agent communication.
