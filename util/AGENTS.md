# Package `util`

This package contains general-purpose utility functions and helpers used throughout the codebase.

## Instruction Utilities (`instructionutil/`)

*   **`InjectSessionState`**: Replaces placeholders like `{key}` with values from the session state, or `{artifact.name}` with artifact content, enabling dynamic instruction templates.
